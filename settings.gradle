import java.util.stream.Stream

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

rootProject.name = 'ForgeGradle'

include 'utils'
include 'base-functional-testing'
include 'base-testing'
include 'base'
include 'common'
include 'vanilla'
include 'mcp'
include 'runs'
//include 'patcher'
include 'userdev'

subProject 'dsl-base'
subProject 'dsl-specification-mcp'
subProject 'dsl-specification-runs'
subProject 'dsl-specification-common'
subProject 'dsl-specification-vanilla'
subProject 'dsl-specification-userdev'

void subProject(final String name) {
    include name
    project(":$name").projectDir = new File(rootDir, "${String.join('/', name.split('-'))}")
}
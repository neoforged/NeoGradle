plugins {
    id 'groovy'
}

dependencies {
    api "org.codehaus.groovy:groovy-all:${project.groovy_version}"
    api gradleApi()

    compileOnly (project(':dsl-annotations'))
    compileOnly (project(':dsl-generator'))
}